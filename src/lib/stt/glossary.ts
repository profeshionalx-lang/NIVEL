/**
 * PADEL_GLOSSARY — словарь падел-терминов для постпроцессинга Whisper-транскриптов.
 *
 * Ключ: канонический термин (строчными буквами).
 * Значение: массив RegExp-паттернов, которые Whisper ошибочно распознаёт
 * вместо этого термина.
 *
 * Паттерны основаны на пилотных артефактах (v3-noprompt.json, 2026-05-13).
 * Флаги gi: глобальная замена + регистронезависимость.
 */
export const PADEL_GLOSSARY: Record<string, RegExp[]> = {
  // Удар бандеха (bandeha) — высокий удар над головой
  бандеха: [
    /бомбех[ауыойе]/gi,
    /пандех[ауыойе]/gi,
    /бамдех[ауыойе]/gi,
    /бандаху/gi,
    /бандею/gi,
    /бандея/gi,
  ],

  // Удар вибора (víbora) — закрученный удар с вращением
  вибора: [
    /версор[ауыойе]/gi,
    /виборa/gi, // латинская "a" вместо кириллической
    /vibora/gi, // латиница
    /виборе/gi,
    /вибор[ыу]/gi,
    /веер/gi,
  ],

  // Ремате (remate) — завершающий смэш-удар
  ремате: [
    /прямат[ауыойе]/gi,
    /рематы/gi,
    /ремата/gi,
    /remata/gi,
    /ремат[ыу]/gi,
  ],

  // Глоба (globo) — высокий защитный удар
  глоба: [
    /глоб[ов]+/gi,
    /глобо/gi,
  ],

  // Аламбре (alambre) — удар от сетки-аламбре
  аламбре: [
    /аламбра/gi,
    /alambre/gi,
    /аламбри/gi,
  ],

  // Чикита (chiquita) — короткий удар у сетки
  чикита: [
    /чек[\s-]?гилл/gi,
    /чикид[ауыойе]/gi,
    /чикита/gi, // уже правильный, но с латиницей
    /chiquita/gi,
  ],

  // Бандехита — маленькая бандеха
  бандехита: [
    /бомбехита/gi,
    /пандехита/gi,
  ],

  // Трахте (trackear) — слежение, позиционирование у задней стены
  трахте: [
    /трахт[ауыойеи]/gi,
    /trackear/gi,
  ],
};
